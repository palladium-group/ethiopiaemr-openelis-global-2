<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
    http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.8.xsd">

    <property name="now" value="now()" dbms="postgresql"/>

    <!--
      Makes the "Histopathology examination" test usable as a program-order result: a sample-type mapping
      (so imported samples get a specimen type instead of a null one, which the case completion needs) and
      a Remark (text) result type (so the pathologist's conclusion is persisted as the result value and can
      be returned to the ordering physician). Both are idempotent and independent of how the test was
      created (2.8.x.x/pathology.xml changeSet 16 or the 026 safety net).
    -->

    <changeSet id="027-pathology-test-sample-type-mapping" author="openelis">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="1">select count(*) from clinlims.test where description = 'Histopathology examination'</sqlCheck>
            <sqlCheck expectedResult="1">select count(*) from clinlims.type_of_sample where description = 'Histopathology specimen'</sqlCheck>
            <sqlCheck expectedResult="0">select count(*) from clinlims.sampletype_test st join clinlims.test t on t.id = st.test_id where t.description = 'Histopathology examination'</sqlCheck>
        </preConditions>
        <comment>Map the Histopathology examination test to the Histopathology specimen sample type</comment>
        <insert schemaName="clinlims" tableName="sampletype_test">
            <column name="id" valueSequenceNext="sample_type_test_seq"/>
            <column name="sample_type_id" valueComputed="(select id from clinlims.type_of_sample where description = 'Histopathology specimen' limit 1)"/>
            <column name="test_id" valueComputed="(select id from clinlims.test where description = 'Histopathology examination' limit 1)"/>
        </insert>
    </changeSet>

    <changeSet id="027-pathology-test-remark-result-type" author="openelis">
        <preConditions onFail="MARK_RAN">
            <sqlCheck expectedResult="1">select count(*) from clinlims.test where description = 'Histopathology examination'</sqlCheck>
            <sqlCheck expectedResult="0">select count(*) from clinlims.test_result tr join clinlims.test t on t.id = tr.test_id where t.description = 'Histopathology examination'</sqlCheck>
        </preConditions>
        <comment>Give the Histopathology examination test a Remark (free-text) result type so the conclusion persists as the result value</comment>
        <insert schemaName="clinlims" tableName="test_result">
            <column name="id" valueSequenceNext="test_result_seq"/>
            <column name="test_id" valueComputed="(select id from clinlims.test where description = 'Histopathology examination' limit 1)"/>
            <column name="tst_rslt_type" value="R"/>
            <column name="sort_order" value="1"/>
            <column name="is_active" value="t"/>
            <column name="lastupdated" valueComputed="${now}"/>
        </insert>
    </changeSet>

</databaseChangeLog>
